# Pokemon Explorer Widget

A React widget that fetches and displays Pokemon from the PokeAPI.

## Setup

```bash
yarn install
```

## Development

```bash
yarn dev
```

## Build

Build the widget first, then update the registry:

```bash
yarn build
cd ../..
./config/build-registry.sh
```

